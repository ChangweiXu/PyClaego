/**
 * bdxTransformer.ts — REMOVED.
 *
 * BDX XML ↔ Tiptap JSON conversion has been migrated to the Python backend.
 * See: src/pyclaego/note_system/tiptap_json_converter.py
 *
 * Frontend now sends/receives Tiptap JSON directly via WebSocket.
 * All BDX format logic lives on the backend.
 */
