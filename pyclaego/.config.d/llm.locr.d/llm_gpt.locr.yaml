# 本地 LLM Router 代理 — GPT 系列模型
#
# 注意：gpt upstream 在 llm_router.yaml 中当前已注释，因此此 provider 也同步注释。
# 如需启用，取消注释本文件并在 llm_router.yaml 中启用对应 upstream。

# locr_gpt5_4:  # GPT-5.4 → 本地代理
#   api: "openai"
#   api_key: "local_router_no_key_needed"
#   base_url: "http://127.0.0.1:18790/v1"
#   model: "gpt5_4"               # llm_router.yaml gpt upstream alias
#   temperature: 1.0
#   max_tokens: 8192
#   max_context_tokens: 180000

{}  # 占位空 dict：gpt upstream 在 llm_router.yaml 中启用后，取消上方注释并删除此行
